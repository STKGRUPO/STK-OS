FROM python:3.13-slim

WORKDIR /app

COPY . .

RUN python -m pip install --no-cache-dir -r apps/api/requirements.txt \
    && python -m pip install --no-cache-dir --no-deps -e apps/api

CMD python -m uvicorn stk_os.main:app --app-dir apps/api/src --host 0.0.0.0 --port "$PORT"
